- Replace instances of next() with some expect() advance
- Finish full parsing
- Fix graph viz
- Allow use of undeclared variables

Questions:
1. If no else, should we still create an empty basic block that falls-through back to the join?