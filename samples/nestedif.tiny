main
var a, b; {
	let a <- call InputNum();
	let b <- call InputNum();
	if a > 0 then
	    if b > 0 then
            call OutputNum(1)
        else
            call OutputNum(0)
        fi
	else
	    call OutputNum(0)
	fi;
	call OutputNum(2)
}.
